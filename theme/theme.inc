<?php

/**
 * Default renderer for the national reports table inside the country profile page
 * @param $variables
 * @return string
 */
function theme_table__country_template_legal_instrument($variables) {
    return theme_table($variables);
}